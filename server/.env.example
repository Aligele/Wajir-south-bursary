# ---- Server ----
PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173

# ---- Supabase ----
SUPABASE_URL=https://enpwvgtgavtfnbhapdcf.supabase.co
# Use the SERVICE ROLE key here (server-side only, never expose to the browser).
# Get it from: Supabase dashboard -> Project Settings -> API -> service_role key
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_DOCS_BUCKET=bursary-docs

# ---- Notifications (optional) ----
# Email via Resend
RESEND_API_KEY=
NOTIFY_FROM_EMAIL=bursary@wajirsouth.go.ke
# SMS via Africa's Talking (leave blank to just log notifications)
AT_API_KEY=
AT_USERNAME=
AT_SENDER_ID=
